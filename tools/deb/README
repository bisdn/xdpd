How to use
==========

Execute as:

	sh# cd build/
	sh# ../tools/deb/checkinstall_gen.sh 
If you want to overwrite the required packages:
	sh# export REQUIRES="<pkg_name1, pkg_name2...>"; ../tools/deb/checkinstall_gen.sh 

Required packages for xdpd/xdpd-dbg pkg
=======================================

Common: 
	- rofl or rofl-dbg - This one is are already added by the script

Plugin dependant:
	- config:
		- Debian 7.X, Ubuntu 14.04 (at least): libconfig++9
	- Add more here..
